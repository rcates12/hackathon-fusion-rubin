/**
 * Site chrome barrel — nav + footer used by the `(site)` route group.
 *
 * These components are demo-site chrome and are NOT the subject of Rubin
 * scans. Planted weaknesses live in the page-body components.
 */

export { SiteNav } from './SiteNav';
export { SiteFooter } from './SiteFooter';
